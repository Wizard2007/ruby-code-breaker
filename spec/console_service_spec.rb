require 'spec_helper'

module Codebreaker
  RSpec.describe ConsoleService, 'Test ConsoleService' do
    subject { ConsoleService.new }
    context '# Test Validate user input ' do
      it 'it should be not empty '
      it 'it shoul be valid console command'
      it 'it shoul be validate by Game '
    end
  end
end